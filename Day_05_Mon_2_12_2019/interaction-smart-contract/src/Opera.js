import React, {useEffect} from "react";
import Web3 from "web3";

const Opera = () => {
    useEffect(() => {
        async function getBlock(){
          
        }
        getBlock();
    },[]);

    return(
        <p>Hello world...</p>
    );
};

export default Opera;